# Contributing

Run `python -m unittest discover -s tests -v` from a checkout. No test dependencies are required.
Please include a minimal synthetic example, expected result, and a regression test for a bug report or fix.
Do not attach private datasets, model credentials, or unpublished results to public issues.
Metric changes must document the estimand, assumptions and comparison with a hand-computed example.
